import styled from "styled-components";

export const ContentPurshase = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 60px 0;

    gap: 40px;
`